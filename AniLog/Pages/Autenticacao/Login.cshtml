@page
@model AniLog.Pages.Autenticacao.LoginModel
@{
    ViewData["Title"] = "Autenticação";
}

<div class="container mt-5" style="max-width: 450px;">
    <div class="card shadow-sm p-4">
        <h3 class="fw-bold mb-1">Entrar no ANILOG</h3>
        <p class="text-muted small mb-4">Insira os seus dados de acesso.</p>

        @if (!string.IsNullOrEmpty(Model.MensagemErro))
        {
            <div class="alert alert-danger py-2 small">@Model.MensagemErro</div>
        }

        <form method="post">
            @Html.AntiForgeryToken()

            <div class="mb-3">
                <label class="form-label fw-semibold">E-mail</label>
                <input type="email" asp-for="Email" class="form-control" autocomplete="email" required />
            </div>
            <div class="mb-4">
                <label class="form-label fw-semibold">Senha</label>
                <input type="password" asp-for="Senha" class="form-control" autocomplete="current-password" required />
            </div>

            <button type="submit" class="btn btn-primary w-100 py-2 shadow-sm mb-3">Autenticar</button>

            <div class="text-center">
                <a asp-page="/Autenticacao/Registrar" class="text-decoration-none small text-muted">Ainda não tem conta? Registe-se</a>
            </div>
        </form>
    </div>
</div>